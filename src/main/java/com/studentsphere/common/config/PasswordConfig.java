package com.studentsphere.common.config;


// Placeholder config kept to avoid package scanning issues.
// Password hashing is implemented directly in AuthService to avoid extra runtime dependencies.
public class PasswordConfig {
    // intentionally empty
}
